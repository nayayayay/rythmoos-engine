/**
 * A DOMHighResTimeStamp (if supported by the browser), or a decimal number representing
 * a timestamp in milliseconds accurate to 5 microseconds. The unit is millisecond.
 */
type HighResTimeStamp = number;

export default HighResTimeStamp;
